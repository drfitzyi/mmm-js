import { describe, it, expect } from 'vitest';

// Smoke test to confirm the test runner is wired up. Replace with real tests in Phase 1+.
describe('scaffold', () => {
  it('runs the test suite', () => {
    expect(1 + 1).toBe(2);
  });
});
